#include <stdio.h>
#include <string.h>

int main(void)
{
  string some_text;
  printf("Enter something what you want: ");
  scanf("@s", &some_text);
  printf("Here we go, "&some_text);

  return 0;
}
